# ============================================================
# .env.example — Copy to .env and fill in real values
# NEVER commit .env to git
# ============================================================

# --- Postgres ---
POSTGRES_DB=scraper_db
POSTGRES_USER=scraper
POSTGRES_PASSWORD=change_me_in_production
# For local dev (direct to Postgres); in Docker point to pgBouncer:
#   DATABASE_URL=postgresql://scraper:change_me@pgbouncer:5432/scraper_db
DATABASE_URL=postgresql://scraper:change_me_in_production@localhost:5432/scraper_db

# --- pgBouncer (Production Hardening 6.4) ---
# Connection pooler sits in front of Postgres.
# In Docker the scraper-api should use port 6432 (pgBouncer), not 5432.
# Set DATABASE_URL above to postgresql://...@pgbouncer:6432/...
PGBOUNCER_MAX_CLIENT_CONN=100
PGBOUNCER_DEFAULT_POOL_SIZE=20

# --- FastAPI Scraper Service ---
SCRAPER_API_HOST=0.0.0.0
SCRAPER_API_PORT=8000
SCRAPER_API_SECRET=change_me_in_production

# --- Next.js Dashboard ---
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXTAUTH_SECRET=change_me_in_production
NEXTAUTH_URL=http://localhost:3000
# Log level for dashboard pino logger (trace|debug|info|warn|error)
LOG_LEVEL=info

# --- n8n Orchestrator ---
# N8N_HOST_PORT: external host port (change if 5678 is taken)
# n8n always listens on 5678 inside the container.
N8N_HOST_PORT=5678
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change_me_in_production
N8N_DB_NAME=n8n_db

# --- Proxy Configuration ---
# Path to JSON file: [{"host":"","port":0,"username":"","password":""}]
PROXY_LIST_PATH=./shared/config/proxies.json
PROXY_VALIDATION_ENABLED=true

# --- Rate Limiting (Phase 5.6) ---
# Default max requests per minute per domain (token bucket)
RATE_LIMIT_RPM=10

# --- CAPTCHA Alerting (Phase 5.4) ---
# n8n webhook URL to receive CAPTCHA-detected events (leave blank to disable)
CAPTCHA_WEBHOOK_URL=http://localhost:5678/webhook/captcha-alert

# --- IP Reputation (Phase 5.5) ---
# proxycheck.io API key (optional — free tier: 1000 checks/day with key)
PROXYCHECK_API_KEY=

# --- Stealth ---
# Deprecated — TLS profiles now rotate automatically (Phase 5.1)
# CURL_IMPERSONATE=chrome120
